# Weather Forecast

A interactive Python command-line utility that fetches live weather reports and activity recommendations for any city worldwide using Open-Meteo's free APIs.

## Requirements

* Python 3.x
* `requests` library

## Installation

1. Clone or download this repository.
2. Install the required Python package:

```bash
pip install requests
